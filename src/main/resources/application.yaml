spring:
  application:
    name: mgw
  datasource:
    url: jdbc:mysql://localhost:3306/mgw
    username: root
    password: hhr120810
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true